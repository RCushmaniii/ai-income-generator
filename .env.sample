# Environment Configuration
NODE_ENV=development

# Analytics (optional)
NEXT_PUBLIC_ANALYTICS_ID=

# Error Tracking (optional)
SENTRY_DSN=

# FX / Currency conversion (optional)
# Used by /api/fx (server-side only). Do NOT expose as NEXT_PUBLIC_*
EXCHANGE_RATE_API_KEY=
